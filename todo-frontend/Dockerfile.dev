# TodoApp/todo-frontend/Dockerfile.dev
FROM node:24.14.0-alpine

WORKDIR /usr/app

COPY package*.json ./
RUN npm ci --legacy-peer-deps

COPY . .

EXPOSE 3000

# Vite dev server binds to 0.0.0.0 by default in config
CMD ["npm", "run", "dev"]
